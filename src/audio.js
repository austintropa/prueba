let context;export function tone(s,type='click'){if(!s.settings.volume)return;try{context??=new AudioContext();const o=context.createOscillator(),g=context.createGain();o.type='square';o.frequency.value=type==='win'?620:type==='hit'?210:380;g.gain.value=.025*s.settings.volume;g.gain.exponentialRampToValueAtTime(.0001,context.currentTime+.12);o.connect(g).connect(context.destination);o.start();o.stop(context.currentTime+.13)}catch{}}
