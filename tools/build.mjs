import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';import {build} from 'esbuild';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const result=await build({entryPoints:[path.join(root,'src/main.js')],bundle:true,write:false,format:'iife',platform:'browser',target:'es2020',minify:true,legalComments:'none'});const js=result.outputFiles[0].text.replace(/<\/script/gi,'<\\/script');const template=await fs.readFile(path.join(root,'tools/index.template.html'),'utf8');await fs.writeFile(path.join(root,'index.html'),template.replace('/* GAME_BUNDLE */',js));console.log(`index.html actualizado: ${Math.round(js.length/1024)} KiB de juego incorporado.`);
